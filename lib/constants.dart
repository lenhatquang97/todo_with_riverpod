import 'package:flutter/material.dart';

const kLargeText = TextStyle(fontSize: 40, color: Colors.white);
const kmiddleText = TextStyle(fontSize: 30, color: Colors.blue);
const kSmallWhiteText = TextStyle(color: Colors.white);
const kradiusSheet = BorderRadius.only(
    topLeft: Radius.circular(20), topRight: Radius.circular(20));
const sizedSpacing = SizedBox(
  height: 10,
);
